import React from 'react';

const Title = ({title}) => (
  <div className="page03-title">
    <i className="page03-title-icon_left"></i>
    <h3 className="page03-title-text">{title}</h3>
    <i className="page03-title-icon_right"></i>
  </div>
);

export default Title;
